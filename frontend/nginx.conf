server {
    listen 80;
    
    # Servir le frontend (HTML/JS)
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # Rediriger les requêtes API vers le conteneur backend
    location /api/ {
        proxy_pass http://backend:5000/;
    }
}